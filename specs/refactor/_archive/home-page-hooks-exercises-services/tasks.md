# Задачи: колокация hooks, exercises и services главной

Ссылка на план: [plan.md](./plan.md).

## Чеклист

- [x] Ввести `src/types/exerciseRuntime.ts`, обновить `utils/pose` и `exercises/types` (импорт `PoseLandmarks` из `utils/pose`).
- [x] Перенести `hooks`, `exercises`, `services` в `src/pages/HomePage/`, поправить импорты (детекторы → `utils`, хуки → `types`/`utils`/`contexts`).
- [x] Обновить документацию с новыми путями.
- [x] `npm run build` и `npm test`.
