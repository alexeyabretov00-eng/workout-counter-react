# Задачи реализации React Router (три страницы)

Связанный план: [react-router-routing-plan.md](./react-router-routing-plan.md).

Используйте чекбоксы по мере выполнения.

---

## Этап A — Зависимости

- [ ] **A1.** Добавить в `package.json` зависимости **`react-router`** и **`react-router-dom`** с **точными** версиями (без `^` / `~`), взятыми из одного релиза пакетов, чтобы версии ядра и DOM-обёртки совпадали.
- [ ] **A2.** Выполнить чистую переустановку: удалить `node_modules`, удалить `package-lock.json`, выполнить `npm install`.

## Этап B — Страницы и маршруты

- [ ] **B1.** Создать `src/pages/` с подпапками `HomePage/`, `AdminPage/`, `ExerciseHistoryPage/` по соглашениям проекта (`index.ts`, явные реэкспорты; стили заглушек — через `styled-components` и тему при необходимости).
- [ ] **B2.** Реализовать **HomePage**: перенести из текущего `src/App.tsx` разметку с `WorkoutLogicLayout` и `AppLayout` (импорты из `./components`, `./containers`, `./logic`).
- [ ] **B3.** Реализовать **AdminPage** и **ExerciseHistoryPage** как заглушки с понятными заголовками (текст вроде «Админка» / «История упражнений»).
- [ ] **B4.** Добавить баррель `src/pages/index.ts` и при необходимости зарегистрировать публичный API (не использовать `export *`).

## Этап C — Интеграция роутера

- [ ] **C1.** Ввести константы путей (например `/`, `/admin`, `/history`) в одном модуле и использовать их в `Route` и `Link`.
- [ ] **C2.** Обернуть приложение в `BrowserRouter` (удобная точка: `src/main.tsx` сразу под `ThemeProvider` или согласованное с командой место).
- [ ] **C3.** В `src/App.tsx` (или выделенном модуле маршрутов) объявить `Routes` / `Route` для трёх страниц; при необходимости обработать неизвестный путь (`Navigate` к `/` или простая заглушка).
- [ ] **C4.** Добавить навигацию между страницами (`Link`), чтобы не зависеть от ручного ввода URL при проверке.

## Этап D — Проверка

- [ ] **D1.** Убедиться, что на `/admin` и `/history` не монтируется `WorkoutLogicLayout` (нет лишней инициализации камеры/сессии на этих экранах).
- [ ] **D2.** Прогнать `npm run lint`, `npm run build`, `npm test`.
- [ ] **D3.** Ручная проверка: главная ведёт себя как раньше; переходы на админку и историю и обратно работают.

---

## Приоритизация

1. A1–A2  
2. B1–B4  
3. C1–C4  
4. D1–D3  
