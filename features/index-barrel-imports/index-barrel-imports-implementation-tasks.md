# Задачи реализации: импорты через index.ts

План: [index-barrel-imports-plan.md](./index-barrel-imports-plan.md)

## Чеклист

- [x] Добавить `index.ts` в `src/hooks`, `src/exercises`, `src/pose`, `src/render` с явным именованным реэкспортом публичных сущностей (без `export *`).
- [x] Обновить импорты в `src` на импорт из папок (через соответствующие `index.ts`).
- [x] Проверить, что после реэкспортов не возникли ошибки сборки/типизации и линтера.
- [x] Обновить документацию для разработчиков (правило импорта через `index.ts`).
- [x] Добавить правило для ИИ в `.cursor/rules` о папочных импортах через `index.ts`.
