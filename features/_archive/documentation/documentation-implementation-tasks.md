# Задачи реализации плана обновления документации

> **Архив:** материалы в `features/_archive/documentation/`. См. `features/README.md`.

Связанный план: [documentation-update-plan.md](./documentation-update-plan.md).

Используйте чекбоксы по мере выполнения.

---

## Этап A — Аудит

- [x] **A1.** Пройти полный пользовательский сценарий в браузере и зафиксировать расхождения с `README.md` (список bullet points для правок).
- [x] **A2.** Сверить `npm` скрипты в `package.json` с разделом «Скрипты» README; отметить отсутствующие или лишние формулировки.
- [x] **A3.** Зафиксировать минимальные требования к браузеру и окружению (камера, микрофон, Speech API, загрузка модели MediaPipe) в черновике для раздела README или `docs/browser-requirements.md`.

## Этап B — README: продукт и пользователь

- [x] **B1.** Обновить блок «Что умеет сейчас»: голос (старт/пауза/сброс/стоп камеры/длительность отдыха), таймер отдыха, озвучка повторений — по факту кода в `App.tsx` и `useWorkoutSession.ts`.
- [x] **B2.** Добавить подраздел «Голосовое управление» с таблицей или списком команд; при перегрузе README — вынести в `docs/voice-commands.md` и дать ссылку.
- [x] **B3.** Описать настройку длительности отдыха (кнопки и/или голос) и поведение отсчёта на canvas.
- [x] **B4.** В «Скрипты» добавить `npm run preview` с одной строкой назначения.

## Этап C — README: разработка

- [x] **C1.** Актуализировать «Архитектура»: роль `src/hooks/useWorkoutSession.ts`, связь `camera` → `services` (landmarker) → `exercises` → отрисовка в `src/utils` (`drawFrame`, `drawRestCountdown`).
- [x] **C2.** Добавить краткую схему потока данных (текстом или mermaid в README — по желанию команды).
- [x] **C3.** Расширить «Как добавить новое упражнение»: ссылка на тесты `src/exercises/__tests__/`, напоминание зарегистрировать детектор в `registry.ts`.

## Этап D — Доп. документы и связи

- [x] **D1.** (Опционально) Создать `docs/voice-commands.md` и перенести туда полный перечень фраз из кода с группировкой по действиям.
- [x] **D2.** (Опционально) Создать `docs/browser-requirements.md` с требованиями и типичными ошибками.
- [x] **D3.** В README одной-двумя фразами сослаться на `todo.md` как на бэклог идей.

## Этап E — Качество

- [x] **E1.** После правок документации выполнить `npm run lint` и `npm run test`; исправить расхождения, если документация ссылалась на неверное поведение.
- [x] **E2.** Ревью README: орфография, единый стиль списков, актуальность версий стека (React, Vite, TypeScript) при необходимости одной строкой в «Стек» или в начале раздела разработки.

---

## Приоритизация (рекомендация)

1. **Сначала:** A1, A2, B1, B4, C1 — максимальный эффект при минимальном объёме.
2. **Затем:** B2, B3, C3, D3.
3. **По мере роста проекта:** D1, D2, E2.
