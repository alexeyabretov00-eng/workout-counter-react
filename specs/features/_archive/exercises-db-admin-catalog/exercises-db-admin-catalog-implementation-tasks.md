# Задачи реализации: справочник упражнений в БД и админ-управление

План: [exercises-db-admin-catalog-plan.md](./exercises-db-admin-catalog-plan.md)

## Чеклист

- [x] Добавить таблицу `exercises` в `server/src/db.ts` и функции доступа к данным (`list`, `create`, `update`, `archive`).
- [x] Добавить первичный набор упражнений в БД для текущих детекторов (`biceps-curl`, `squat`, `army-press`, `head-side-tilt`).
- [x] Реализовать `GET /api/exercises` для выдачи списка упражнений в клиент.
- [x] Реализовать админ-endpoints управления упражнениями (`POST/PATCH/DELETE` или эквивалентный soft-delete контракт).
- [x] Добавить валидацию payload (обязательные поля, типы, `detectorKey`, сортировка, уникальные ограничения).
- [x] Добавить API-клиент упражнений в `src/api` с типами DTO и обработкой ошибок.
- [x] Подключить список упражнений из API в `HomeModule` (контейнеры/селекторы) вместо прямого чтения `exerciseRegistry` для UI options.
- [x] Сопоставить элементы справочника с локальными детекторами по `detectorKey`; предусмотреть безопасное поведение для неизвестного ключа.
- [x] Обновить `AdminModule`: список упражнений, создание, редактирование, архивирование.
- [x] Обновить `README.md` по источнику списка упражнений и управлению через админку.
- [x] Прогнать `npm run lint`.
- [x] Прогнать `npm run test`.
