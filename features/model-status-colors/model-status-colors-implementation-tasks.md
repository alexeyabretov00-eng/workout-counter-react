# Задачи: цветовые статусы модели

Связанный план: [model-status-colors-plan.md](./model-status-colors-plan.md).

---

- [ ] **1.** Добавить в `useWorkoutSession` явное состояние модели и различать `loading`, `ready` и `error`.
- [ ] **2.** Пробросить новое состояние модели в `App.tsx`, сохранив текущую блокировку запуска до готовности.
- [ ] **3.** Обновить строку состояния в `App.tsx`, чтобы показывать тексты `загружается`, `загружена` и `не загружена`.
- [ ] **4.** Добавить в `App.css` отдельные стили для `.model-state.loading`, `.model-state.ready` и `.model-state.error`.
- [ ] **5.** Проверить, нужна ли правка документации по строке состояния, и выполнить минимальную верификацию после изменений.
