# Задачи: текст статуса паузы

Связанный план: [pause-status-text-plan.md](./pause-status-text-plan.md).

---

- [x] **1.** Добавить флаг паузы в `useWorkoutSession` и корректно обновлять его в `pause`, `start` и `stopSession`.
- [x] **2.** Пробросить флаг из `useWorkoutSession` в `App.tsx`.
- [x] **3.** В `App.tsx` отобразить текст `Упражнение приостановлено` при активной паузе.
- [x] **4.** В `App.tsx` и `App.css` заменить показ последнего кадра на текстовый экран в области `.stage` во время паузы.
